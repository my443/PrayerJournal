<Window x:Class="PrayerJournal.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PrayerJournal"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ToolBarTray Grid.ColumnSpan="3" Grid.Column="0">
            <ToolBar>
                <Button x:Name="Add" BorderBrush="#FF390A0A" Click="Add_Click">
                    <Label>Add Item</Label>
                </Button>
                <Button x:Name="Save" BorderBrush="#FF390A0A">
                    <Label>Save Item</Label>
                </Button>
                <Button x:Name="Delete" BorderBrush="#FF390A0A">
                    <Label>Delete Item</Label>
                </Button>

            </ToolBar>
        </ToolBarTray>
        <TabControl x:Name="tabControl" Grid.Column="0" Grid.RowSpan="3" Grid.Row="1" SelectionChanged="tabControl_SelectionChanged">
            <TabItem Header="Current">
                <ListBox x:Name="listboxCurrentItems"  Width="801" >
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}{0}, {1}">
                                        <Binding Path="Summary" />
                                        <Binding Path="CreatedDate" />
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </TabItem>
            <TabItem Header="History">
                <Grid Background="#FFE5E5E5">
                    <ListBox x:Name="listboxHistoryItems" Width="801">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock>
                                    <TextBlock.Text>
                                        <MultiBinding StringFormat="{}{0}, {1}">
                                            <Binding Path="Summary" />
                                            <Binding Path="CreatedDate" />
                                        </MultiBinding>
                                    </TextBlock.Text>
                                </TextBlock>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </TabItem>
        </TabControl>
        <GridSplitter Grid.Column="1" 
                      VerticalAlignment="Stretch" 
                      HorizontalAlignment="Stretch" 
                      FontSize="16" 
                      Width="5" 
                      Grid.RowSpan="3"
                      Grid.Row="1" IsTabStop="False"/>
        <TextBox x:Name ="textboxSummary" 
                 Text="{Binding ElementName=listboxCurrentItems, Path=SelectedItem.Summary}" 
                    Grid.Row="1" Grid.Column="2" FontSize="18" GotFocus="textboxSummary_GotFocus"/>
        <TextBox x:Name="textboxDescription"
                 Text="{Binding ElementName=listboxCurrentItems, Path=SelectedItem.Description}"
                 AcceptsReturn="True" 
                 AcceptsTab="True" 
                 HorizontalAlignment="Stretch" 
                 TextWrapping="Wrap" 
                 VerticalAlignment="Stretch" 
                 Width="Auto" 
                 Grid.Row="2" 
                 Grid.Column="2"/>
        <StackPanel Grid.Row="3" Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <StackPanel HorizontalAlignment="Left" Orientation="Horizontal">
                <Label>Is A Historical Item</Label>
                <CheckBox VerticalAlignment="Center"></CheckBox>
            </StackPanel>
            <Label >Date Created</Label>
            <DatePicker HorizontalAlignment="Right"/>
        </StackPanel>

    </Grid>
</Window>
